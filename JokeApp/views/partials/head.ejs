<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Your Joke Website</title>
  <style>
    /* Hide the default multiple-select dropdown */
    select {
      display: none;
    }

    /* Style the label to create a custom radio button */
    .custom-radio {
      display: inline-block;
      cursor: pointer;
      padding: 5px;
      border: 1px solid #ccc;
      margin: 5px;
    }

    /* Style the selected custom radio button */
    .custom-radio input:checked + label {
      background-color: #3498db;
      color: #fff;
    }
  </style>
</head>
<body>